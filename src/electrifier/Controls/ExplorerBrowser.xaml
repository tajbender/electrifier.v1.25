<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="electrifier.Controls.ExplorerBrowser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:electrifier.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <CommandBar x:Name="CommandBar"
                    Grid.ColumnSpan="2"
                    Grid.Row="0"
                    DefaultLabelPosition="Collapsed"
                    HorizontalAlignment="Left">
            <AppBarButton Icon="Back" Label="Navigate back" />
            <AppBarButton Icon="Forward" Label="Navigate forward" />
            <AppBarButton Icon="Up" Label="Parent folder" />
            <AppBarSeparator />
            <AppBarButton Icon="OpenFile"
                          Label="New">
                <AppBarButton.Flyout>
                    <Flyout>
                        <StackPanel>
                            <AppBarButton Icon="Folder"
                                          IsEnabled="False" 
                                          Label="Folder"/>
                            <AppBarButton Icon="Link"
                                          IsEnabled="False" 
                                          Label="Shortcut"/>
                            <AppBarSeparator />
                        </StackPanel>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarSeparator />
            <AppBarButton Icon="Cut"
                          IsEnabled="False"
                          Label="Cut" />
            <AppBarButton Icon="Copy"
                          IsEnabled="False"
                          Label="Copy" />
            <AppBarButton Icon="Paste"
                          IsEnabled="False"
                          Label="Paste" />
            <AppBarButton Icon="Rename"
                          IsEnabled="False"
                          Label="Rename" />
            <AppBarButton Icon="Share"
                          IsEnabled="False"
                          Label="Share" />
            <AppBarButton Icon="Delete"
                          IsEnabled="False"
                          Label="Delete" />
            <AppBarButton Icon="Undo" Label="Undo" Visibility="Collapsed" />
            <AppBarSeparator />
            <AppBarButton Icon="Pin" Label="Pin to Quick access" Visibility="Collapsed"/>
            <AppBarSeparator />
            <AppBarButton Icon="SelectAll" Label="Select all" />
            <!-- TODO: The Icon="ViewAll" is the one, that Windows Explorer uses for `Select none` -->
<!--            <AppBarButton Icon="ClearSelection" Label="Select none" Visibility="Collapsed" />
            <AppBarButton Icon="Warning" Label="Invert selection" Visibility="Collapsed" />
            <AppBarButton Icon="Warning" Label="Properties" Visibility="Collapsed"/>
            <AppBarButton Icon="Setting" Label="Options" Visibility="Collapsed" /> -->
            <AppBarSeparator/>
            <AppBarElementContainer>
                <SplitButton x:Name="SortSplitButton"
                             Content="Sort">
                    <SplitButton.Flyout>
                        <Flyout>
                            <StackPanel>
                                <Button Content="Name"
                                        IsEnabled="False" />
                                <Button Content="Date Modified"
                                        IsEnabled="False" />
                                <Button Content="Type"
                                        IsEnabled="False" />
                                <Button Content="~"
                                        IsEnabled="False" />
                                <Button Content="More"
                                        IsEnabled="False" />
                                <Button Content="~"
                                        IsEnabled="False" />
                                <Button Content="Ascending"
                                        IsEnabled="False" />
                                <Button Content="Descending"
                                        IsEnabled="False" />
                                <Button Content="~"
                                        IsEnabled="False" />
                                <Button Content="Group by"
                                        IsEnabled="False" />
                            </StackPanel>
                        </Flyout>
                    </SplitButton.Flyout>
                    <SplitButton.Resources>
                        <TeachingTip x:Name="SortSplitButtonTeachingTip"
                                     Title="TODO: Add Sort and Group options."
                                     Subtitle="Use the dropdown button option to save your item and create another."
                                     Target="{x:Bind SortSplitButton}" />
                    </SplitButton.Resources>
                </SplitButton>
            </AppBarElementContainer>
            <AppBarElementContainer>
                <SplitButton x:Name="ViewSplitButton"
                             Content="View">
                    <SplitButton.Flyout>
                        <Flyout>
                            <StackPanel>
                                <Button Content="Tiles"
                                        IsEnabled="False" />
                                <Button Content="Content"
                                        IsEnabled="False" />
                                <Button Content="~"
                                        IsEnabled="False" />
                                <Button Content="Details pane"
                                        IsEnabled="False" />
                                <Button Content="File Manager pane"
                                        IsEnabled="False" />
                                <Button Content="~"
                                        IsEnabled="False" />
                                <Button Content="Show"
                                        IsEnabled="False" />
                                <Button Content="~"
                                        IsEnabled="False" />
                                <Button Content="Two Pages"
                                        IsEnabled="False" />
                                <!--  Glyph: E89A, TwoPage  -->
                                <AppBarButton Icon="TwoPage"
                                              Label="Double pane mode"
                                              IsEnabled="False" />
                                <AppBarButton Icon="Refresh"
                                              Label="Refresh"
                                              IsEnabled="False" />
                                <AppBarButton Icon="Orientation"
                                              Label="Orientation"
                                              IsEnabled="False" />
                                <Button Content="Spy Mode - Chiffre Filenames"
                                        IsEnabled="False" />
                                <Button Content="File Extensions: Use default (Windows Explorer settings)"
                                        IsEnabled="False" />
                                <Button Content="Files Extensions: Show for uncommon files only Hide"
                                        IsEnabled="False" />
                                <Button Content="Files Extensions: Show for uncommon files only"
                                        IsEnabled="False" />
                            </StackPanel>
                        </Flyout>
                    </SplitButton.Flyout>
                </SplitButton>
            </AppBarElementContainer>

            <AppBarButton Icon="View" Label="View">
                <AppBarButton.Flyout>
                    <Flyout>
                        <StackPanel>
                            <AppBarButton Icon="View" Label="Tiles" />
                            <AppBarButton Icon="View" Label="Details" />
                            <MenuFlyoutSeparator/>
                            <AppBarButton Icon="View" Label="Compact Mode" />
                            <MenuFlyoutSeparator/>
                            <AppBarButton Icon="View" Label="Show hidden Files" />
                        </StackPanel>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>


            <AppBarButton Icon="Filter" Label="Filter">
                <AppBarButton.Flyout>
                    <Flyout>
                        <StackPanel>
                            <AppBarButton Icon="Important" Label="Hide folders" />
                            <AppBarButton Icon="Important" Label="Hide Files" />
                            <AppBarButton Icon="Important"
                                          Label="Important" />
                        </StackPanel>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarElementContainer>
                <SplitButton x:Name="FilterSplitButton"
                             Content="Filter">
                    <SplitButton.Flyout>
                        <Flyout>
                            <StackPanel>
                                <RadioButtons>
                                    <RadioButton Content="Show all Folders"></RadioButton>
                                    <RadioButton Content="Hide all Folders"></RadioButton>
                                    <RadioButton Content="Hide hidden Folders"></RadioButton>
                                </RadioButtons>
                                <AppBarSeparator />
                                <RadioButtons>
                                    <RadioButton Content="All Files"></RadioButton>
                                    <RadioButton Content="No Hidden Folders"></RadioButton>
                                    <RadioButton Content="Hide Folders"></RadioButton>
                                </RadioButtons>
                                <AppBarSeparator />
                                <RadioButtons>
                                    <RadioButton Content="Always show Thumbnails"></RadioButton>
                                    <RadioButton Content="Fast mode (Use Stock Icons only)"></RadioButton>
                                </RadioButtons>
                            </StackPanel>
                        </Flyout>
                    </SplitButton.Flyout>
                    <SplitButton.Resources>
                        <TeachingTip x:Name="FilterSplitButtonTeachingTip"
                                     Title="TODO: Add filter options title text."
                                     Subtitle="Use the dropdown button option to save your item and create another."
                                     Target="{x:Bind FilterSplitButton}" />
                    </SplitButton.Resources>
                </SplitButton>
            </AppBarElementContainer>
            <AppBarSeparator />

            <AppBarButton Icon="View" Label="View">
                <AppBarButton.Flyout>
                    <Flyout>
                        <StackPanel>
                            <AppBarButton Icon="View" Label="Tiles" />
                            <AppBarButton Icon="View" Label="Details" />
                            <MenuFlyoutSeparator/>
                            <AppBarButton Icon="View" Label="Compact Mode" />
                            <MenuFlyoutSeparator/>
                            <AppBarButton Icon="View" Label="Show hidden Files" />
                        </StackPanel>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>



            <AppBarElementContainer>
                <SplitButton Content="File´s Tags">
                    <SplitButton.Flyout>
                        <Flyout>
                            <StackPanel>
                                <AppBarButton Icon="Home" Label="Home" />
                                <AppBarButton Icon="Edit" Label="Work" />
                                <AppBarButton Icon="Pictures" Label="Photos" />
                                <AppBarButton Icon="Important" Label="Important" />
                            </StackPanel>
                        </Flyout>
                    </SplitButton.Flyout>
                </SplitButton>
            </AppBarElementContainer>
            <CommandBar.SecondaryCommands>
                <AppBarButton Icon="Undo"
                              Label="Undo"
                              IsEnabled="False" />
                <AppBarSeparator />
                <AppBarButton Icon="Pin"
                              Label="Pin to Quick access"
                              IsEnabled="False" />
                <AppBarSeparator />
                <AppBarButton Icon="SelectAll"
                              Label="Select all"
                              IsEnabled="False" />
                <!-- TODO: The Icon="ViewAll" is the one, that Windows Explorer uses for `Select none` -->
                <AppBarButton Icon="ClearSelection"
                              Label="Select none"
                              IsEnabled="False" />
                <AppBarButton Icon="Shuffle"
                              Label="Invert selection"
                              IsEnabled="False" />
                <AppBarSeparator />
                <AppBarButton Icon="Repair"
                              Label="Properties"
                              IsEnabled="False" />
                <AppBarButton Icon="Setting"
                              Label="Options"
                              IsEnabled="False" />
            </CommandBar.SecondaryCommands>
        </CommandBar>
        <SplitView x:Name="PrimarySplitView"
                   Grid.Column="0"
                   Grid.Row="1"
                   PaneBackground="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}"
                   IsPaneOpen="True"
                   OpenPaneLength="192"
                   DisplayMode="Inline">
            <SplitView.Pane>
                <local:ShellNamespaceTreeControl x:Name="PrimaryShellTreeView" />
            </SplitView.Pane>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <local:ShellListView x:Name="PrimaryShellListView"
                                     Grid.Row="0" />
                <CommandBar x:Name="PrimaryStatusCommandBar"
                            Grid.Column="0"
                            Grid.Row="1"
                            HorizontalAlignment="Left"
                            HorizontalContentAlignment="Left"
                            VerticalAlignment="Stretch"
                            VerticalContentAlignment="Center">
                    <AppBarButton IsCompact="True">
                        <TextBlock>
                            <Run FontWeight="SemiBold"
                                 Text="Items: " />
                        </TextBlock>
                    </AppBarButton>
                    <AppBarSeparator />
                    <AppBarButton IsCompact="True">
                        <TextBlock>
                            <Run FontWeight="SemiBold"
                                 Text="Folders: " />
                        </TextBlock>
                    </AppBarButton>
                    <AppBarSeparator />
                    <AppBarButton IsCompact="True">
                        <TextBlock>
                            <Run FontWeight="SemiBold"
                                 Text="Files: " />
                        </TextBlock>
                    </AppBarButton>
                    <AppBarSeparator />
                    <AppBarButton IsCompact="True">
                        <TextBlock>
                            <Run FontWeight="SemiBold"
                                 Text="Size: " />
                            <Run Text="KB" />
                        </TextBlock>
                    </AppBarButton>
                    <AppBarSeparator />
                    <AppBarButton IsCompact="True">
                        <TextBlock>
                            <Run FontWeight="SemiBold"
                                 Text="Hidden: " />
                        </TextBlock>
                    </AppBarButton>
                </CommandBar>
            </Grid>
        </SplitView>
        <Border x:Name="SidebarBorder"
                Grid.Column="1"
                Grid.Row="1"
                MinWidth="192"
                BorderThickness="7"
                Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
        </Border>
    </Grid>
</UserControl>
